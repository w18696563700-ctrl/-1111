---
owner: Codex 总控
status: draft
purpose: Freeze the discussion boundary for detail boundary only within Rating bigger-loop planning without unlocking contracts, L3 truth, or implementation.
layer: L0 SSOT
---

# Rating detail boundary 讨论边界补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It serves only:
  - `detail boundary only`
- It freezes:
  - the discussion-inside boundary
  - the discussion-outside boundary
  - the current formal non-approval statement
  - the next allowed document-freeze direction
- It does not by itself:
  - rewrite `docs/00_ssot/rating_bigger_loop_planning_addendum.md`
  - rewrite `docs/00_ssot/rating_bigger_loop_discussion_goal_addendum.md`
  - approve detail capability
  - approve detail implementation
  - approve detail contract

## Current Board Name
- Current board:
  - `Rating bigger-loop planning`

## Current Single Discussion Goal Relation
- The current single discussion goal remains:
  - `detail boundary only`
- This addendum exists only to freeze the formal discussion boundary for that
  single goal.
- This addendum does not expand the current round into:
  - `history boundary only`
  - `list boundary only`
  - `review or moderation boundary only`
  - `richer scoring or feedback-model boundary only`
  - `downstream dispute-link boundary only`

## Discussion Inside Boundary
- Inside the current discussion boundary, the round may discuss only whether a
  later round needs one independent detail-facing boundary for `Rating`.
- Inside the current discussion boundary, the round may discuss only how a
  later detail-facing boundary would relate to the existing minimum
  `Rating` loop:
  - `GET /api/app/rating/entry`
  - `POST /api/app/rating/submit`
- Inside the current discussion boundary, the round may discuss only how a
  later detail-facing boundary would relate to the already frozen:
  - `rating_object_decision_addendum.md`
  - `rating_truth_readability_final_ruling.md`
- Inside the current discussion boundary, the round may discuss only which
  questions must stay deferred until a later round if detail is ever reopened,
  including questions that belong to:
  - later contracts work
  - later `L3` consumer-boundary work
  - later implementation work

## Discussion Outside Boundary
- Outside the current discussion boundary:
  - detail implementation
  - detail app-facing path
  - detail schema or field freeze
  - detail state machine
  - detail moderation
  - detail history
  - detail list
  - richer scoring or feedback-model
  - downstream dispute-link
- Outside the current discussion boundary also remains:
  - any `L2 Contracts` freeze
  - any `L3 BFF truth` freeze
  - any `L3 Frontend truth` freeze
  - any backend / BFF / frontend implementation prompt

## Formal Non-approval Statement
- This is not detail capability approval.
- This is not detail implementation approval.
- This is not detail contract approval.
- This file must not be misread as saying the next round may directly develop
  detail.

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- The current document is frozen only as:
  - `detail boundary only` discussion-boundary truth
- After this file, the next step may freeze only:
  - detail explicit non-goals
  - or detail future re-entry gate path
- After this file, the round may not directly enter implementation.

## Next Unique Action
- Freeze either the detail explicit non-goals document or the detail future
  re-entry gate-path document, but not implementation.
