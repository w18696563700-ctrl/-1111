---
owner: Codex 总控
status: draft
purpose: Freeze the current discussion-goal switch to review or moderation boundary only within Rating bigger-loop planning after the detail, history, and list L0 planning packages have already been completed.
layer: L0 SSOT
---

# Rating review or moderation discussion goal 补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It freezes only:
  - the current discussion-goal switch
  - the fact that `detail boundary only`, `history boundary only`, and
    `list boundary only` L0 planning packages are already complete
  - the current single discussion goal for the next planning subpackage
- It does not by itself:
  - reopen `detail`
  - reopen `history`
  - reopen `list`
  - approve `review or moderation`
  - approve implementation
  - approve contracts
  - approve `L3` truth

## Current Board Name
- Current board remains:
  - `Rating bigger-loop planning`

## Prior Package Status Before This Switch
- The `detail boundary only` planning package is already complete at `L0`
  planning truth.
- The `history boundary only` planning package is already complete at `L0`
  planning truth.
- The `list boundary only` planning package is already complete at `L0`
  planning truth.
- Total control has already decided:
  - do not apply for `detail` re-entry
  - do not apply for `history` re-entry
  - do not apply for `list` re-entry

## Current Discussion Goal Switch
- The current single discussion goal is now formally switched to:
  - `review or moderation boundary only`
- This switch is:
  - a planning-goal switch only
  - not a review or moderation capability approval
  - not a review or moderation implementation approval
  - not a review or moderation contract approval

## Current Non-expansion Rule
- This switch does not automatically open:
  - `richer scoring or feedback-model`
  - `downstream dispute-link`

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- Current single discussion goal is now:
  - `review or moderation boundary only`

## Next Unique Action
- Freeze the formal discussion boundary for
  `review or moderation boundary only` under `docs/**` only.
