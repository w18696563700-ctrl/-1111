---
owner: Codex 总控
status: draft
purpose: Freeze the current discussion-goal switch to downstream dispute-link boundary only within Rating bigger-loop planning after the detail, history, list, review-or-moderation, and richer-scoring-or-feedback-model L0 planning packages have already been completed.
layer: L0 SSOT
---

# Rating downstream dispute-link discussion goal 补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It freezes only:
  - the current discussion-goal switch
  - the fact that `detail boundary only`, `history boundary only`,
    `list boundary only`, `review or moderation boundary only`, and
    `richer scoring or feedback-model boundary only` L0 planning packages are
    already complete
  - the current single discussion goal for the next planning subpackage
- It does not by itself:
  - reopen `detail`
  - reopen `history`
  - reopen `list`
  - reopen `review or moderation`
  - reopen `richer scoring or feedback-model`
  - approve `downstream dispute-link`
  - approve implementation
  - approve contracts
  - approve `L3` truth
  - reopen `Dispute`

## Current Board Name
- Current board remains:
  - `Rating bigger-loop planning`

## Prior Package Status Before This Switch
- The `detail boundary only` planning package is already complete at `L0`
  planning truth.
- The `history boundary only` planning package is already complete at `L0`
  planning truth.
- The `list boundary only` planning package is already complete at `L0`
  planning truth.
- The `review or moderation boundary only` planning package is already
  complete at `L0` planning truth.
- The `richer scoring or feedback-model boundary only` planning package is
  already complete at `L0` planning truth.
- Total control has already decided:
  - do not apply for `detail` re-entry
  - do not apply for `history` re-entry
  - do not apply for `list` re-entry
  - do not apply for `review or moderation` re-entry
  - do not apply for `richer scoring or feedback-model` re-entry

## Current Discussion Goal Switch
- The current single discussion goal is now formally switched to:
  - `downstream dispute-link boundary only`
- This switch is:
  - a planning-goal switch only
  - not a downstream dispute-link capability approval
  - not a downstream dispute-link implementation approval
  - not a downstream dispute-link contract approval
  - not a `Dispute` reopening approval

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- Current single discussion goal is now:
  - `downstream dispute-link boundary only`

## Next Unique Action
- Freeze the formal discussion boundary for
  `downstream dispute-link boundary only` under `docs/**` only.
