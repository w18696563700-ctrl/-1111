---
owner: Codex 总控
status: draft
purpose: Freeze the explicit non-goals for detail boundary only within Rating bigger-loop planning as binding prohibitions for the current round.
layer: L0 SSOT
---

# Rating detail explicit non-goals 补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It serves only:
  - `detail boundary only`
- It freezes the current explicit non-goals as binding prohibitions for the
  current round.
- It does not by itself:
  - rewrite `docs/00_ssot/rating_bigger_loop_planning_addendum.md`
  - rewrite `docs/00_ssot/rating_bigger_loop_discussion_goal_addendum.md`
  - rewrite `docs/00_ssot/rating_detail_boundary_discussion_addendum.md`
  - approve detail capability
  - approve detail implementation
  - approve detail contract
  - approve future re-entry by implication

## Current Board Name
- Current board:
  - `Rating bigger-loop planning`

## Current Single Discussion Goal Relation
- The current single discussion goal remains:
  - `detail boundary only`
- This addendum exists only to freeze the explicit non-goals for that single
  discussion goal.
- This addendum does not expand the current round into:
  - `history boundary only`
  - `list boundary only`
  - `review or moderation boundary only`
  - `richer scoring or feedback-model boundary only`
  - `downstream dispute-link boundary only`

## Current Explicit Non-goals
- The current round does not approve:
  - detail implementation
  - detail app-facing path
  - detail schema or field freeze
  - detail state machine
  - detail review or moderation
  - detail history
  - detail list
  - richer scoring or feedback-model
  - downstream dispute-link
  - any backend dispatch
  - any `BFF` dispatch
  - any frontend dispatch
  - any `L2 Contracts`
  - any `L3 BFF truth`
  - any `L3 Frontend truth`

## Binding Interpretation
- The above non-goals are current-round hard constraints.
- They are not “not written yet”.
- They are not “implementation can proceed first”.
- They mean:
  - the current round is forbidden from advancing those items
  - the current round may not smuggle those items in through planning wording
  - the current round may not reinterpret discussion as approval

## Re-entry Rule
- If any one of the above non-goals is later proposed to be reopened, it must
  go through a new formal re-entry gate.
- No later round may treat this file as prior approval for:
  - detail implementation
  - detail contract freeze
  - `L3` consumer-boundary freeze
  - history / list / moderation / richer scoring / dispute-link expansion

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- The current document is frozen only as:
  - `detail boundary only` explicit non-goals truth
- After this file, the next step may freeze only:
  - `detail future re-entry gate path`
- After this file, the round may not directly enter:
  - implementation
  - contract freeze
  - `L3` truth freeze

## Next Unique Action
- Freeze the `detail future re-entry gate path` document only.
