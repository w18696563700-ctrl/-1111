---
owner: Codex 总控
status: draft
purpose: Freeze the ranked discussion candidates and the single current discussion goal for Rating bigger-loop planning without unlocking contracts, L3 truth, or implementation.
layer: L0 SSOT
---

# Rating bigger-loop planning 唯一讨论目标冻结单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It freezes:
  - the ranked discussion candidates
  - the single current discussion goal
  - the current explicit non-goals
  - the currently allowed next freeze range
  - the currently forbidden next freeze range
- It does not by itself:
  - create a second startup addendum
  - rewrite the existing
    `docs/00_ssot/rating_bigger_loop_planning_addendum.md`
  - approve implementation
  - approve `L2 Contracts`
  - approve `L3` truth freeze

## Current Board Name
- Current board:
  - `Rating bigger-loop planning`

## Current Ranked Discussion Candidates
- The current ranked discussion candidates are frozen as:
  1. `detail boundary only`
  2. `history boundary only`
  3. `list boundary only`
  4. `review or moderation boundary only`
  5. `richer scoring or feedback-model boundary only`
  6. `downstream dispute-link boundary only`
- These candidates remain discussion scope only.
- Their ranking does not equal implementation approval.

## Current Single Discussion Goal
- The only current discussion goal is:
  - `detail boundary only`

## Why Detail Boundary Goes First
- `detail boundary only` is the smallest bigger-loop discussion object in the
  current candidate set.
- It is the most controllable current discussion object because it can be
  bounded around one read-facing planning topic without automatically pulling
  in broader workflow expansion.
- It is the least likely current discussion object to drift into parallel
  multi-object planning.
- Freezing `detail boundary only` first does not automatically open:
  - `history boundary only`
  - `list boundary only`
  - `review or moderation boundary only`
  - `richer scoring or feedback-model boundary only`
  - `downstream dispute-link boundary only`

## Current Explicit Non-goals
- No implementation
- No `L2 Contracts`
- No `L3 BFF truth`
- No `L3 Frontend truth`
- No backend dispatch
- No `BFF` dispatch
- No frontend dispatch
- No multi-object parallel planning
- No reinterpretation of `detail boundary only` as “next round directly develop
  detail”

## Currently Allowed Next Freeze Range
- The current round may continue freezing only:
  - the formal discussion boundary for `detail boundary only`
  - the explicit non-goals for `detail boundary only`
  - the future re-entry gate path for `detail boundary only`

## Currently Forbidden Next Freeze Range
- The current round may not continue freezing:
  - detail implementation
  - detail app-facing path
  - detail schema
  - moderation implementation approval
  - history implementation approval
  - list implementation approval
  - richer scoring or feedback-model implementation approval
  - downstream dispute-link implementation approval

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- Current single discussion goal is formally frozen as:
  - `detail boundary only`

## Next Unique Action
- Continue by freezing the formal discussion boundary for
  `detail boundary only` under `docs/**` only.
