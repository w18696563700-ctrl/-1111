---
owner: Codex 总控
status: draft
purpose: Freeze the discussion boundary for downstream dispute-link boundary only within Rating bigger-loop planning without unlocking contracts, L3 truth, implementation, or the Dispute main flow.
layer: L0 SSOT
---

# Rating downstream dispute-link boundary 讨论边界补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It serves only:
  - `downstream dispute-link boundary only`
- It freezes:
  - the discussion-inside boundary
  - the discussion-outside boundary
  - the current formal non-approval statement
- It does not by itself:
  - approve downstream dispute-link capability
  - approve downstream dispute-link implementation
  - approve downstream dispute-link contract
  - reopen the `Dispute` main flow

## Current Board Name
- Current board remains:
  - `Rating bigger-loop planning`

## Current Single Discussion Goal Relation
- The current single discussion goal remains:
  - `downstream dispute-link boundary only`
- This addendum exists only to freeze the formal discussion boundary for that
  single goal.

## Discussion Inside Boundary
- Inside the current discussion boundary, the round may discuss only whether
  `Rating` later needs one independent downstream-dispute-link boundary.
- Inside the current discussion boundary, the round may discuss only how a
  later downstream-dispute-link boundary would relate to the existing minimum
  `Rating` loop:
  - `GET /api/app/rating/entry`
  - `POST /api/app/rating/submit`
- Inside the current discussion boundary, the round may discuss only how a
  later downstream-dispute-link boundary would relate to the already frozen:
  - `dispute_object_decision_addendum.md`
  - `dispute_entry_minimal_governance_action_addendum.md`
- Inside the current discussion boundary, the round may discuss only which
  questions must stay deferred until a later round if downstream dispute-link
  is ever reopened, including questions that belong to:
  - later contracts work
  - later `L3` consumer-boundary work
  - later implementation work

## Discussion Outside Boundary
- Outside the current discussion boundary:
  - downstream dispute-link implementation
  - downstream dispute-link app-facing path
  - downstream dispute-link schema
  - downstream dispute-link state machine
  - reopening the `Dispute` main flow
  - detail
  - history
  - list
  - review or moderation
  - richer scoring or feedback-model
- Outside the current discussion boundary also remains:
  - any `L2 Contracts`
  - any `L3 BFF truth`
  - any `L3 Frontend truth`
  - any backend / `BFF` / frontend implementation prompt

## Formal Non-approval Statement
- This is not downstream dispute-link capability approval.
- This is not downstream dispute-link implementation approval.
- This is not downstream dispute-link contract approval.
- This is not approval to reopen the `Dispute` main flow.

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- The current document is frozen only as:
  - `downstream dispute-link boundary only` discussion-boundary truth

## Next Unique Action
- Freeze the explicit non-goals for
  `downstream dispute-link boundary only`.
