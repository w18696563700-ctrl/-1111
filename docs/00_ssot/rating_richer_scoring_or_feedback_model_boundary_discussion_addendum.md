---
owner: Codex 总控
status: draft
purpose: Freeze the discussion boundary for richer scoring or feedback-model boundary only within Rating bigger-loop planning without unlocking contracts, L3 truth, or implementation.
layer: L0 SSOT
---

# Rating richer scoring or feedback-model boundary 讨论边界补充单

## Scope
- This addendum applies only to the current
  `Rating bigger-loop planning` round.
- It serves only:
  - `richer scoring or feedback-model boundary only`
- It freezes:
  - the discussion-inside boundary
  - the discussion-outside boundary
  - the current formal non-approval statement
- It does not by itself:
  - approve richer scoring or feedback-model capability
  - approve richer scoring or feedback-model implementation
  - approve richer scoring or feedback-model contract

## Current Board Name
- Current board remains:
  - `Rating bigger-loop planning`

## Current Single Discussion Goal Relation
- The current single discussion goal remains:
  - `richer scoring or feedback-model boundary only`
- This addendum exists only to freeze the formal discussion boundary for that
  single goal.

## Discussion Inside Boundary
- Inside the current discussion boundary, the round may discuss only whether a
  later round needs one independent richer-scoring-or-feedback-model-facing
  boundary for `Rating`.
- Inside the current discussion boundary, the round may discuss only how a
  later richer-scoring-or-feedback-model-facing boundary would relate to the
  existing minimum `Rating` loop:
  - `GET /api/app/rating/entry`
  - `POST /api/app/rating/submit`
- Inside the current discussion boundary, the round may discuss only how a
  later richer-scoring-or-feedback-model-facing boundary would relate to the
  already frozen:
  - `rating_object_decision_addendum.md`
  - `rating_truth_readability_final_ruling.md`
- Inside the current discussion boundary, the round may discuss only which
  questions must stay deferred until a later round if richer scoring or
  feedback-model is ever reopened, including questions that belong to:
  - later contracts work
  - later `L3` consumer-boundary work
  - later implementation work

## Discussion Outside Boundary
- Outside the current discussion boundary:
  - richer scoring or feedback-model implementation
  - richer scoring or feedback-model app-facing path
  - richer scoring or feedback-model schema
  - richer scoring or feedback-model state machine
  - detail
  - history
  - list
  - review or moderation
  - downstream dispute-link
- Outside the current discussion boundary also remains:
  - any `L2 Contracts`
  - any `L3 BFF truth`
  - any `L3 Frontend truth`
  - any backend / `BFF` / frontend implementation prompt

## Formal Non-approval Statement
- This is not richer scoring or feedback-model capability approval.
- This is not richer scoring or feedback-model implementation approval.
- This is not richer scoring or feedback-model contract approval.

## Formal Conclusion
- Current board remains:
  - `Rating bigger-loop planning`
- Current maximum allowed level remains:
  - `L0 SSOT planning truth`
- The current document is frozen only as:
  - `richer scoring or feedback-model boundary only`
    discussion-boundary truth

## Next Unique Action
- Freeze the explicit non-goals for
  `richer scoring or feedback-model boundary only`.
