# Admin Skeleton

Phase 0 scope:
- enterprise review
- project review
- template configuration
- audit logs
- basic ticketing

Routing principle:
- `Admin` uses controlled `Server` Admin APIs.
- `Admin` does not go through `BFF`.
